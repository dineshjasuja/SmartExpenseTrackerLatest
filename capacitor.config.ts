import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dinesh.smartspend',
  appName: 'smartspend-expense-tracker',
  webDir: 'dist'
};

export default config;
